const { get, post, mainHandler, use, decorateIO, staticFileHandler, bodyParser } = require('./utils');

module.exports = {
  use,
  get,
  post,
  mainHandler,
  decorateIO,
  staticFileHandler,
  bodyParser,
};
