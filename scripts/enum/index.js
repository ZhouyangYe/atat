const COMMANDS = {
  BUILD: 'build',
  DEV: 'dev',
};

const MODE = {
  DEV: 'development',
  PROD: 'production',
};

const appList = ['algorithms', 'hahaha', 'home', 'intro', 'notFound', 'pingpong'];
const common = 'common';

module.exports = {
  COMMANDS,
  MODE,
  appList,
  common,
};
