const buildAll = require('./buildAll');
const buildSome = require('./buildSome');
const cleanApps = require('./cleanApps');
const updatePassword = require('./genPassword');

module.exports = {
  buildAll,
  buildSome,
  cleanApps,
  updatePassword,
};
